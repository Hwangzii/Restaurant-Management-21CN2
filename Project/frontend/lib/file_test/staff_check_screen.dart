import 'package:flutter/material.dart';

class StaffCheckScreenTest extends StatelessWidget {
  const StaffCheckScreenTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('demo điểm danh nhân viên'),
      ),
    );
  }
}